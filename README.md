# Python Simple Image Downloader

- Put your URLs in the array.
- Optional: Change the destination folder name/path.
- Run the script.

Images will be downloaded to the destination folder. If it doesn't exist, it will be created.
